export function StepTemplateBox(isMarginTop) {
    return isMarginTop ? {
        marginTop: 2
    } : {
            padding: 3,
            textAlign: 'center'
        }
}

export const TypographyStyles = {
    fontFamily: 'Inter, system-ui, sans-serif',
    fontWeight: 'bold'
}